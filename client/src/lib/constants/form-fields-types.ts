export enum FormFieldsTypes {
  INPUT = 'input',
  TEXTAREA = 'textarea',
  PHONE_INPUT = 'phoneInput',
  SELECT = 'select',
  CHECKBOX = 'checkbox',
  DATE_PICKER = 'datePicker',
  CUSTOM = 'custom',
}